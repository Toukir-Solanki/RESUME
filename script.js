document.addEventListener('DOMContentLoaded', (() => { const e = document.getElementById("loaderScreen"), t = document.getElementById("loaderBar"), n = document.querySelectorAll(".loader-letter"), l = n.length, a = 250, o = 600, r = 100 / l; n.forEach(((e, n) => { setTimeout((() => { e.classList.add("show"), t.style.width = (n + 1) * r + "%" }), 400 + n * a) })); const i = 400 + l * a + o; setTimeout((() => { e.classList.add("done"), document.body.classList.remove("is-loading"), setTimeout(s, 500) }), i); function s() { document.querySelectorAll(".animate-item").forEach(((e, t) => { const n = 150 * parseInt(e.dataset.delay || t) + 100; setTimeout((() => e.classList.add("show")), n) })) } const c = document.getElementById("typingText"), m = ["systems.", "networks.", "solutions.", "automation."]; let d = 0, u = 0, g = !1; !function e() { const t = m[d]; g ? (c.textContent = t.substring(0, u - 1), u--, 0 === u && (g = !1, d = (d + 1) % m.length, setTimeout(e, 400)) || setTimeout(e, 50)) : (c.textContent = t.substring(0, u + 1), u++, u === t.length && (g = !0, setTimeout(e, 2e3)) || setTimeout(e, 90)) }(), setTimeout((() => { const e = document.getElementById("cursorGlow"); e && window.innerWidth > 768 && document.addEventListener("mousemove", (t => { requestAnimationFrame((() => { e.style.left = t.clientX + "px", e.style.top = t.clientY + "px" })) })) }), 1500); const y = document.getElementById("navbar"); let f = 0; window.addEventListener("scroll", (() => { const e = window.scrollY; e > 50 ? y.classList.add("scrolled") : y.classList.remove("scrolled"), f = e }), { passive: !0 }); const h = document.getElementById("hamburger"), v = document.getElementById("navLinks"), b = document.createElement("div"); function E() { h.classList.toggle("active"), v.classList.toggle("open"), b.classList.toggle("active"), document.body.style.overflow = v.classList.contains("open") ? "hidden" : "" } b.className = "nav-overlay", document.body.appendChild(b), h.addEventListener("click", E), b.addEventListener("click", E), v.querySelectorAll("a").forEach((e => { e.addEventListener("click", (() => { v.classList.contains("open") && E() })) })); const L = document.querySelectorAll(".tab-btn"), k = document.querySelectorAll(".tab-panel"); L.forEach((e => { e.addEventListener("click", (() => { L.forEach((e => { e.classList.remove("active"), e.setAttribute("aria-selected", "false") })), k.forEach((e => e.classList.remove("active"))), e.classList.add("active"), e.setAttribute("aria-selected", "true"); const t = e.getAttribute("data-tab"); document.getElementById(`panel-${t}`).classList.add("active") })) })), document.querySelectorAll(".reveal").forEach((e => new IntersectionObserver((e => { e.forEach((e => { e.isIntersecting && (e.target.classList.add("visible"), this.unobserve(e.target)) })) }), { threshold: .12, rootMargin: "0px 0px -60px 0px" }).observe(e))), document.querySelectorAll('a[href^="#"]').forEach((e => { e.addEventListener("click", (t => { t.preventDefault(); const n = document.querySelector(e.getAttribute("href")); n && n.scrollIntoView({ behavior: "smooth", block: "start" }) })) })); const w = document.querySelectorAll("section[id]"), S = document.querySelectorAll(".nav-links ol a"); w.forEach((e => new IntersectionObserver((e => { e.forEach((e => { if (e.isIntersecting) { S.forEach((e => e.classList.remove("nav-active"))); const t = document.querySelector(`.nav-links a[href="#${e.target.id}"]`); t && t.classList.add("nav-active") } })) }), { threshold: .3 }).observe(e))) }));
